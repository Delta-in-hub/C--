#include "token.h"

extern std::vector<Token> tokenArr{};

void scan(char* buf)
{
    char* p = buf;

    while (*p)
    {
        //跳过空格,注释//  or  /*

        //标识符
        //关键字

        // symbols
        //字符常量
        //数字常量 -> int/double
        //字符串常量
    }
}